{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="w-100 text-end">
        <button class="btn btn-success"><i class="fa-regular fa-floppy-disk"></i></button>
    </div>
{{ form_end(form) }}
